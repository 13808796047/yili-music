CREATE TABLE user (
    id VARCHAR (32) NOT NULL PRIMARY KEY COMMIT '用户ID',
    username VARCHAR (64) NOT NULL COMMIT '用户名',
    password VARCHAR (64) NOT NULL COMMIT '性别',
    gender VARCHAR (255) NULL COMMIT '性别',
    email VARCHAR (255) NULL

)